{
  "name": "contorno",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "dev": "partykit dev",
    "deploy": "partykit deploy"
  },
  "devDependencies": {
    "partykit": "^0.0.115"
  }
}
