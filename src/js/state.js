let pool      = [];

// Modo Perguntas / Continentes
let gs = {};
let gZoom, gSvgSel;

// Modo Livre
let fs = {};
let fZoom, fSvgSel;

// Modo Disputa
let ds = {};
let dZoom, dSvgSel;

// Modo Localizar
let ls = {};
let lZoom, lSvgSel;

// User selections
let selectedLevel          = 1;
let selectedContinent      = 'sul';
let selectedDuelLevel      = 1;
let selectedLocalizarLevel = 1;

let isOnlineMode = false;
